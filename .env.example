# memory-agent — agent-specific env vars
# Shared vars come from /opt/agents-common.env
PORT=3013
OLLAMA_MODEL=petedio-planner
SURREAL_URL=http://localhost:8001
SURREAL_NS=petedio
SURREAL_DB=homelab
SURREAL_USER=root
# SURREAL_PASS is injected from a sealed secret at deploy time
KNOWLEDGE_ROOT=/home/pedro/PeteDio-Labs/knowledge/sessions
